import 'package:flutter/material.dart';

// Constante que define o padding de todas as linhas / exercícios de alfabeto.
const double rowLeftPadding = 8.0;

// Variável para aviso de erro na tela de Alfabeto
var alfaMessage = '';
Color alfaMessageColor = Colors.transparent; // Para a cor da mensagem
Color alfaContainerColor = Colors.transparent; // Cor do container da mensagem

// Variável para aviso de erro na tela de Numeros
var numMessage = '';
Color numMessageColor = Colors.transparent; // Para a cor da mensagem
Color numContainerColor = Colors.transparent; // Cor do container da mensagem

